version: '3.7'
services:
  postgres:
    image: postgres:11.1
    ports:
      - "5432"
  web:
    build: .
    env_file:
      - .env
    command: bash -c "(bundle check || bundle install) && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/reserva2
    ports:
      - "3000:3000"
    depends_on:
      - postgres
